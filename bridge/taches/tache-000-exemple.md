# Tâche 000 (exemple — ne pas traiter)

> Ce fichier est un modèle de référence. La session cloud l'ignore car il possède
> déjà un fichier `tache-000-exemple.resultat.md` marqué comme exemple.

## Objectif
Montrer le format attendu d'un fichier de tâche.

## Instructions détaillées
1. Décris ici les étapes précises, comme si la personne qui exécute n'avait aucun
   contexte sur ta machine ni sur le projet.
2. Si la tâche a besoin d'un fichier d'entrée, dépose-le dans `bridge/` et
   référence-le par son chemin dans le dépôt (ex. `bridge/entrees/mon-fichier.csv`).

## Résultat attendu
Décris le livrable : un résumé, un fichier, un message envoyé, etc.

## Livraison
Où le résultat doit atterrir (ex. `bridge/livrables/` + fichier `.resultat.md`).
